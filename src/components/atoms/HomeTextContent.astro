---
const { title, text } = Astro.props;
---

<div class="flex flex-col items-center">
  <div class="w-[50vw] my-8">
    <div>{title}</div>
    <div class="w-[40vw] ml-[5vw] whitespace-pre-line">{text}</div>
  </div>
</div>
