---
const { title, type } = Astro.props;
---

<div class="w-full flex flex-row space-x-6">
  <div class="w-[clamp(10px,8vw,100px)] mt-2">{title}</div>
  <input
    type={type}
    class="w-[80%] h-[3dvh] border-b border-b-black"
    required
  />
</div>
